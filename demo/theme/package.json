{
  "name": "theme-demo",
  "private": true,
  "description": "Theme demo for vitepress-theme-hope",
  "license": "MIT",
  "author": "Mr.Hope",
  "scripts": {
    "demo:serve": "http-server -s -h localhost -d dist",
    "demo:build": "vitepress build src",
    "demo:clean-dev": "vitepress dev src --clean-cache",
    "demo:dev": "vitepress dev src",
    "demo:default-build": "cross-env DEFAULT=TRUE vitepress build src",
    "demo:default-clean-dev": "cross-env DEFAULT=TRUE vitepress dev src --clean-cache",
    "demo:default-dev": "cross-env DEFAULT=TRUE vitepress dev src"
  },
  "devDependencies": {
    "vitepress-theme-default": "workspace:*",
    "vitepress-theme-hope": "workspace:*",
    "vitepress": "1.0.0-beta.1"
  }
}
